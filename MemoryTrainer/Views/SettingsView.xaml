<UserControl x:Class="MemoryTrainer.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource BackgroundBrush}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="← Back" Style="{StaticResource SecondaryButton}" Command="{Binding GoBackCommand}"/>
            <TextBlock Grid.Column="1" Text="Settings" FontSize="18" FontWeight="Bold"
                       VerticalAlignment="Center" Margin="12,0,0,0"/>
        </Grid>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Default Session Name Template" Style="{StaticResource Label}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding DefaultSessionNameTemplate, UpdateSourceTrigger=PropertyChanged}"
                     Width="300" HorizontalAlignment="Left" Margin="0,0,0,16"/>

            <Button Content="Delete All Screenshots" Style="{StaticResource DangerButton}"
                    Command="{Binding DeleteAllScreenshotsCommand}"
                    HorizontalAlignment="Left" Margin="0,0,0,8"/>

            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SuccessBrush}"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToCollapsedConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>
